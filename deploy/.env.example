# 复制成 .env 后,执行 bash gen-secrets.sh 自动填入随机密码。
# ⚠️ .env 绝不能提交到 Git(已在 .gitignore 里)

# Let's Encrypt 证书通知邮箱
ACME_EMAIL=support@yunmengapi.com

# PostgreSQL —— 密码由脚本生成,不要手写
PG_USER=yunmeng
PG_DB=newapi
PG_PASSWORD=

# Redis
REDIS_PASSWORD=

# new-api 会话与加密密钥,泄露等于账户全失守
SESSION_SECRET=
CRYPTO_SECRET=
